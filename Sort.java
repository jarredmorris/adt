/**
 * An interface to sort an array of elements of type T
 * @param <T> the type of the elements being sorted
 */
public interface Sort<T> {

	/**
	 * Sort the array (by reference)
	 */
	public void sort();
}
